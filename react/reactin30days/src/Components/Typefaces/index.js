import React from 'react'
import './index.css'

export const Title = (properties) => {
  return (
    <span className = 'typerfaces title h1' >
      {properties.children}
    </span>
  )
}

export const Body14 = (properties) => {
  return (
    <span className = 'typerfaces title body14' >
      {properties.children}
    </span>
  )
}

export const Title32m = (properties) => {
  return (
    <span className = 'typerfaces title h1' >
      {properties.children}
    </span>
  )
}